# Core dependencies
python-osc>=1.8.0
websockets>=11.0
numpy>=1.24.0
scipy>=1.10.0
aiohttp>=3.8.0
requests>=2.28.0
pyyaml>=6.0
watchdog>=3.0.0

# Audio I/O (sounddevice works on macOS M-series without PortAudio manual build)
sounddevice>=0.4.6
soundfile>=0.12.0

# Optional: PyAudio (requires PortAudio — use 'brew install portaudio' on macOS)
# pyaudio>=0.2.13

# Audio processing
pyloudnorm>=0.1.1
librosa>=0.10.0

# ML (PyTorch — use CPU build for Apple Silicon)
torch>=2.0.0

# AI / RAG
chromadb>=0.4.0

# Voice control (optional — comment out if not needed)
# faster-whisper>=1.0.0
rapidfuzz>=3.0.0

# Monitoring & logging
prometheus-client>=0.16.0
structlog>=23.0.0

# Mastering (optional)
matchering>=2.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
